const tasks = ["Task1", "Task2", "Task3", 12, true];
console.log(tasks.length);
console.log(tasks[2]); // access index 2 values

//
const cartItems = ["book1", "book2", "book3"];
let result = `You have ${cartItems.length} products in your cart`;
console.log(result);
console.log(`First item is ${cartItems[0]}`);
console.log(`First item is ${cartItems[cartItems.length - 1]}`);
